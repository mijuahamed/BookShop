@model IEnumerable<BookModel>

        <div class="row">
            <h1>Top Books:</h1>


            @foreach (var book in Model)
            {

                <div class="col-md-4">
                    <partial name="_bookThumbnail" model="@book" />
                </div>
            }


        </div>
